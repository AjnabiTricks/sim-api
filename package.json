{
  "name": "fast-phone-search-api",
  "version": "1.0.0",
  "description": "Fast phone/CNIC search API with caching and batch support",
  "main": "api/search.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "start": "node api/search.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
